services:
  postpilot:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - DATABASE_URL=sqlite:////data/postpilot.db
    volumes:
      - postpilot-data:/data
    restart: unless-stopped

volumes:
  postpilot-data:
